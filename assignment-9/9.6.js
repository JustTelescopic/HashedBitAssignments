
      // Object with properties for name, age, and occupation
      const person = {
        name: "John",
        age: 30,
        occupation: "Engineer"
      };

      // Function that logs a greeting message
      function greetPerson(person) {
        console.log(`Hello, my name is ${person.name}, I am ${person.age} years old, and I work as a ${person.occupation}.`);
      }

      greetPerson(person);
    